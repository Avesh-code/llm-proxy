# LLM Proxy — runtime dependencies
#
# Version floors matter here:
#   arize-phoenix-otel  — register() builds a Phoenix-aware OTLP TracerProvider
#                         (endpoint/headers/protocol/api_key wiring). Verified
#                         working on 0.17.1.
#   openinference-semantic-conventions — attribute name constants (SpanAttributes,
#                         OpenInferenceSpanKindValues) used when tagging spans.
#   opentelemetry-sdk / opentelemetry-exporter-otlp-proto-http — pulled in by
#                         arize-phoenix-otel; pinned here for clarity. Verified
#                         working on 1.44.0.
#   httpx   >=0.27 — async streaming client used for the upstream call.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
arize-phoenix-otel>=0.17,<1.0
openinference-semantic-conventions>=0.1.38,<1.0
opentelemetry-sdk>=1.44,<2.0
opentelemetry-exporter-otlp-proto-http>=1.44,<2.0
