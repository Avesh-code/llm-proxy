# Langfuse AI Proxy — runtime dependencies
#
# Version floors matter here:
#   langfuse >=3  — proxy.py uses get_client(), propagate_attributes() and
#                   start_as_current_observation(), which do not exist in 2.x.
#                   Verified working on 4.15.
#   httpx   >=0.27 — async streaming client used for the upstream call.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
langfuse>=3.0,<5.0
pyyaml>=6.0,<7.0
